var loginButton = document.getElementById("login-button").clicked;
var userId = ["jcp0048", "testUser"]
var userIdValue = document.getElementById("user-id-input").value

function checkId() {
  if (loginButton == true) {
    if () {

    } else {
      block of code to be executed
      if the condition is false
    }
  }
}
  button.addEventListener("enter", change)
